<nav>
  <ol class="flex flex-row py-3">
    <li class="text-darkBlue font-accent px-2 text-sm font-bold uppercase align-baseline">
      {% if pagination.href.previous %}
        <a href="{{ pagination.href.previous }}">Previous</a>
        {% else %}Previous{% endif %}
    </li>
    <li class="text-darkBlue font-accent px-2 text-sm font-bold uppercase align-baseline">
      {% if pagination.href.next %}
        <a href="{{ pagination.href.next }}">Next</a>
        {% else %}Next{% endif %}
    </li>
  </ol>
</nav>